{% extends 'base.html.twig' %}

{% block title %}Advice{% endblock %}

{% block body %}
    <h1>Advice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advice.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ advice.message }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ advice.name }}</td>
            </tr>
            <tr>
                <th>Response</th>
                <td>{{ advice.response }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('advice_index') }}">back to list</a>

    <a href="{{ path('advice_edit', {'id': advice.id}) }}">edit</a>

    {{ include('advice/_delete_form.html.twig') }}
{% endblock %}
